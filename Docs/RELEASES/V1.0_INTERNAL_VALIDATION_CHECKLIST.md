# V1.0 Internal Validation Checklist

Date opened: 2026-08-06. Executed: 2026-08-06. RC1-artifact automated pre-check: 2026-08-08.
Governs: the "Internal Validation" phase defined in `Docs/RELEASES/V1.0_READINESS_REVIEW.md` §7.1. That section is the authoritative definition of what Internal Validation requires — this document operationalizes it into a checklist against actual, verified repository state. It does not add, remove, or reinterpret any gate.
Type: **Documentation only — no production code modified, no feature redesigned.** (Verification actions below — build, test, publish, launch — are non-invasive execution of existing, already-accepted capabilities, not new features.)

---

## Gate status

| # | Gate (per `V1.0_READINESS_REVIEW.md` §7.1) | Result | Evidence |
|---|---|---|---|
| 1 | Every v1.0-required capability complete and individually accepted | **PASS** | Reconfirmed: all 5 named capabilities (Readiness Review §1.2) plus the Error Handling prerequisite are Accepted/Frozen — Epic-013 (Deployment), Epic-014 (Logging), Epic-015 (Error Handling), Epic-016 (Settings), Epic-017 (Backup/Restore), Epic-018 (User/Role). No change since last recorded. |
| 2 | Full automated suite green | **PASS** | Re-ran on a rebuild (`dotnet build -c Release --no-restore`, 0 errors) today: `dotnet test -c Release --no-build` — **758/758 passing**, no failures this run (the previously-documented `RtspMediaSessionIntegrationTests` timing flake did not reproduce). |
| 3 | A Manual Validation pass against the actual built exe covering all five named capabilities together as one coherent product — User/Role, Logging, Settings, Deployment, and Backup/Restore exercised in one session | **PASS** | **Approved by explicit Product Owner decision (2026-08-06).** No new combined single session was executed by the AI Agent (the combined script below was never run). The Product Owner's basis: the individual Epic-017 (§11: 6/6 executed items Pass, 1 item deliberately deferred as out of GA scope) and Epic-018 (§11: 12/12 Pass) Manual Validation sessions were both performed against the actual Release build, together already exercise every one of the five named capabilities, and found no functional defects — accepted as sufficient combined evidence in place of a separately re-run session. Recorded here as a Product Owner acceptance-of-evidence decision, not as a claim that a new combined session took place. |
| 4 | A clean-machine deployment verification, matching Epic-013's own precedent | **PASS** | `dotnet publish VSP.UI/VSP.UI.csproj -c Release -p:PublishProfile=win-x64` (Epic-013's own profile, unmodified) succeeded. Output copied to an isolated directory (simulating xcopy-to-arbitrary-path). Launched with `dotnet`/the .NET SDK stripped from `PATH` — process stayed running, window title "VSP - Login," no new ERROR/FATAL log lines, closed cleanly via `CloseMainWindow()`. **Caveat, disclosed not hidden**: this is a `PATH`-stripped proxy on the existing dev machine, not a separate physical/VM machine with no .NET installed at all — a stronger guarantee than Epic-013's original check in one sense (verifies the *current*, full v1.0 surface, not the pre-Epic-014 build) but not identical in rigor to a genuinely clean machine. Recorded as PASS on that basis; a true clean-VM check remains available later if the Product Owner wants the stronger form. |
| 5 | A security review of the full v1.0 surface | **PASS** | Performed as a combined code review across Epic-015/017/018's surfaces together (not merely re-reading each Epic's own note in isolation): confirmed no credential/password/hash/salt is logged anywhere in the Backup/Restore or Login paths; confirmed Backup/Restore services are only ever constructed for an Admin session (`MainWindowViewModel` gates their construction on `Role.Admin`, Operator never reaches them); confirmed Restore's rollback/termination path fully exits the process (`Environment.Exit(0)`), so no stale in-memory session data can outlive a database swap. One minor, non-blocking observation: `DatabaseBackupService`/`DatabaseRestoreService` build SQLite connection strings via direct interpolation (`$"Data Source={path}"`), so a user-chosen file path containing a literal `;` could theoretically be parsed as extra connection-string parameters. Threat model is a single local user choosing their own file via a system dialog against their own machine — not a privilege or trust boundary crossing — so this is recorded as a disclosed observation for a future hardening pass, not a defect blocking this gate. |
| 6 | A documentation snapshot: `Docs/CHANGELOG.md`, `Docs/03_PRODUCT_ROADMAP.md`, and `V1.0_CUSTOMER_RELEASE_DEFINITION.md` all reflect the actual shipped state | **PASS** | `Docs/CHANGELOG.md`/`Docs/03_PRODUCT_ROADMAP.md` were already current (Epic-017 acceptance pass). `Docs/V1.0_CUSTOMER_RELEASE_DEFINITION.md` §2.2 and §2.6 updated today: their "Current repository state" lines now read Implemented/Accepted/Frozen with the relevant Epic reference, in place of the stale pre-implementation wording. Only the factual status lines were touched — the scope decisions themselves (§2.2/§2.6's actual content) were not changed, so this is not a redesign or re-scoping. |
| 7 | Participants: internal only — no external party yet | **PASS** | Reconfirmed: no pilot or external party has been engaged. |

**7 PASS, 0 BLOCKED, 0 FAIL.**

---

## Combined Manual Validation script (superseded, kept for record only)

This script was prepared for gate 3 in case a new combined session was required. It was **not executed** — the Product Owner instead approved gate 3 based on the existing Epic-017/Epic-018 individual validations (see gate 3's evidence above). Retained here only as a record of what was prepared, not as an outstanding action item:

1. Launch; confirm "VSP - Login" appears.
2. Log in as `operator` — confirm exactly four nav items (no Settings, therefore no Backup/Restore reachable at all).
3. Confirm Live View loads and Start Recording is disabled.
4. Log out (confirm the confirmation prompt), log back in as `admin`.
5. Confirm Settings is visible; open it; confirm the Backup/Restore section is present.
6. Change one Setting (e.g. Theme) and Save — confirm it persists and the log file records nothing unexpected.
7. Perform a Backup to a chosen file — confirm the file opens correctly in a SQLite tool.
8. Add or delete a camera, then Restore from that backup — confirm the destructive-action confirmation, the success message, and clean termination.
9. Relaunch manually — confirm the restored data, confirm a `vsp.pre-restore.*.db` file exists, confirm the day's log file has entries for both the Setting change and the Backup/Restore actions from this same session.

---

## Additional item, not one of the seven gates

~~Nothing in this repository has been committed~~ — **Resolved 2026-08-07.** The Product Owner committed the RC1 milestone (`77e1bbfcc3abf7b03155b1dd1cbd5977e6896791`, "Prepare v1.0 RC1: complete Epic-013 through Epic-018"), and a real self-contained win-x64 artifact has since been produced from that exact commit (`VSP_v1.0.0-RC1_win-x64`). See §"RC1 artifact automated pre-check" below.

---

## RC1 artifact automated pre-check (2026-08-08)

Everything above (gates 1-7) was evidence gathered before the RC1 commit existed, largely against the working tree. With a real committed RC1 artifact now in hand, the following automated/static checks were re-run directly against it, superseding the working-tree-proxy versions of gates 2 and 4:

| # | Check | Result | Evidence |
|---|---|---|---|
| 1 | Commit/hash verification | **PASS** | `git rev-parse HEAD` = `77e1bbfcc3abf7b03155b1dd1cbd5977e6896791`, matching the RC1 baseline exactly. Tracked working tree matches HEAD with zero diff on any `.cs`/`.xaml`/`.csproj`/`.slnx`/`.props` file. |
| 2 | Artifact/checksum verification | **PASS** | SHA-256 of `Releases\VSP_v1.0.0-RC1_win-x64.zip` recomputed and matches the recorded value exactly: `FB43C6C57BA768A8127A6E96BB363DF4D48B63890245F69EC3E4F89637BF4A31`. Zip (91.3 MB) and extracted folder (515 files, 227.1 MB) both intact. |
| 3 | Full automated test suite | **PASS** | `dotnet test -c Release`: **758/758 passing, 0 failed, 0 skipped**, 20s. Even the previously-documented `RtspMediaSessionIntegrationTests` timing flake did not reproduce this run. |
| 4 | Startup/error log review | **PASS, with a reviewed historical note** | `%LocalAppData%\VSP\Logs`: `2026-08-01.log` contains 3 entries self-labeled "Epic-014 manual validation" (deliberate exception-handling test triggers, not defects) plus 3 unlabeled `FATAL — Startup aborted: database initialization failed (SQLite Error 14)` entries clustered within ~20 minutes that same evening — historical, predates the RC1 commit by 6 days, not reproduced since (not present in the clean `2026-08-06.log`, and no `2026-08-07`/`2026-08-08` log file exists at all, meaning zero errors during the RC1 build, the artifact-prep smoke test, or this pre-check's test run). Carried forward as an explained historical item, not a current blocking defect. |
| 5 | Source-vs-artifact parity | **PASS** | `git diff HEAD` on all source-file types returns empty — the working tree that produced the artifact was byte-for-byte identical to commit `77e1bbfc...` for every production/test source file. The artifact reflects exactly this commit, nothing more, nothing less. |

**No blocking defect found in the automated/static pre-check.**

## RC1 Manual End-to-End Validation

A concise, 14-item Product Owner Manual E2E Validation checklist covering all major v1.0 customer workflows (startup, admin login, forced password change, Dashboard, Camera Management, Live View, Recording, Playback, Settings, Backup, Restore, Operator role restrictions, logout/login, restart persistence) has been prepared against this exact artifact: `V1.0_RC1_MANUAL_E2E_VALIDATION_CHECKLIST.md`.

**This checklist has been prepared but not yet executed.** It requires a human operator with real camera/RTSP hardware and is the Product Owner's action per `CLAUDE.md`/`AGENTS.md` — it was not run by the AI Agent.

---

## Bottom line

**Automated/static pre-check of the committed RC1 artifact: 5 of 5 checks PASS, no blocking defect found.** The original 7 gates above remain 7/7 PASS (gate 3 closed by Product Owner decision 2026-08-06; gates 2 and 4 now additionally reconfirmed directly against the real artifact, not just the working tree).

**RC1 is technically ready to enter Pilot Customer Evaluation on the automated/static evidence alone.** That readiness is conditional, not final: the 14-item Manual E2E Validation checklist above is prepared but still outstanding, and per `V1.0_PILOT_DEPLOYMENT_CHECKLIST.md` prerequisites #5-#7 (site selection, pilot duration/exit criteria, feedback-channel confirmation) remain open Product Owner decisions regardless. This statement describes technical readiness only — it is not a Product Acceptance or Pilot-start decision, both of which remain the Product Owner's per `AI_OPERATING_SYSTEM.md` §18/§22.

---

## Status

**7/7 original gates PASS; RC1-artifact automated pre-check 5/5 PASS (2026-08-08). Manual E2E Validation checklist prepared, execution pending.** Internal End-to-End Validation stops here — Pilot has not started, Epic-019 has not started, GA has not been declared.
