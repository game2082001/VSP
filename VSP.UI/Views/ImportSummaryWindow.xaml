<Window x:Class="VSP.UI.Views.ImportSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="760"
        Height="520"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Import Summary"
                   FontSize="24"
                   FontWeight="SemiBold"/>

        <Grid Grid.Row="1"
              Margin="0,20,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Total"/>
            <TextBlock Grid.Column="1" Text="Imported"/>
            <TextBlock Grid.Column="2" Text="Skipped"/>
            <TextBlock Grid.Column="3" Text="Failed"/>

            <TextBlock Grid.Row="1"
                       Margin="0,8,0,0"
                       Text="{Binding TotalRows}"/>
            <TextBlock Grid.Row="1"
                       Grid.Column="1"
                       Margin="0,8,0,0"
                       Text="{Binding ImportedRows}"/>
            <TextBlock Grid.Row="1"
                       Grid.Column="2"
                       Margin="0,8,0,0"
                       Text="{Binding SkippedRows}"/>
            <TextBlock Grid.Row="1"
                       Grid.Column="3"
                       Margin="0,8,0,0"
                       Text="{Binding FailedRows}"/>
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Errors}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Row"
                                    Binding="{Binding RowNumber}"
                                    Width="80"/>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="180"/>
                <DataGridTextColumn Header="Message"
                                    Binding="{Binding Message}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Close"
                    Width="96"
                    Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</Window>
